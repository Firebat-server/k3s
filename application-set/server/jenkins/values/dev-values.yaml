chartConfig:
  repoURL: https://charts.jenkins.io
  chart: jenkins
  targetRevision: 5.8.116

persistence:
  enabled: true
  existingClaim: jenkins-pvc

controller:
  ingress:
    enabled: true
    ingressClassName: traefik
    hostName: jenkins.jay-gemini.com
