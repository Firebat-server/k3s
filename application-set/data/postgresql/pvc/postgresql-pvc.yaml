apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql-dev-master-pvc
spec:
  storageClassName: postgresql-dev-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      type: local
