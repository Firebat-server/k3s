apiVersion: v1
kind: Service
metadata:
  name: {{ include "jay-blog-be.fullname" . }}
  labels:
    {{- include "jay-blog-be.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "jay-blog-be.selectorLabels" . | nindent 4 }}
