apiVersion: v1
kind: Service
metadata:
  name: {{ include "jay-blog-fe.fullname" . }}
  labels:
    {{- include "jay-blog-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "jay-blog-fe.selectorLabels" . | nindent 4 }}
