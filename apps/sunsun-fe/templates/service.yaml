apiVersion: v1
kind: Service
metadata:
  name: { { include "sunsun-fe.fullname" . } }
  labels: { { - include "sunsun-fe.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "sunsun-fe.selectorLabels" . | nindent 4 } }
