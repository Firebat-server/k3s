apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sunsun-fe.fullname" . }}-test-connection"
  labels:
    {{- include "sunsun-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sunsun-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
